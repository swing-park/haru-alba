{
  "name": "haru-alba",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "seed": "node scripts/seed.mjs"
  },
  "devDependencies": {
    "bcryptjs": "^3.0.3",
    "mongodb": "^7.1.0",
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=20"
  }
}
